{
  "version": "v1",
  "hooks": [
    {
      "name": "Testes antes de commit",
      "trigger": "PreToolUse",
      "matcher": "execute_pwsh",
      "action": {
        "type": "agent",
        "prompt": "Se o comando que está prestes a ser executado é um 'git commit', primeiro rode 'npm test' no projeto-demo e só prossiga com o commit se todos os testes passarem."
      }
    }
  ]
}
